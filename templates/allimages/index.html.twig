<!doctype html>

<html lang="ru">
<head>
  <meta charset="utf-8">

  <title>Отображение всех ссылок на картинки со страницы | Тестовое задание №2</title>
  <meta name="description" content="Тестовое задание №2">
  <meta name="author" content="Ivan Lebedev">

   <link href="{{ asset('css/styles.css') }}" rel="stylesheet" />

</head>

<body>
<section>
  <h1>Ссылки на картинки со страницы {{url}}</h1>
  <div class="tbl-header">
    <table cellpadding="0" cellspacing="0" border="0">
      <thead>
        <tr>
          <th>Номер</th>
          <th>URL</th>
        </tr>
      </thead>
    </table>
  </div>
  <div class="tbl-content">
    <table cellpadding="0" cellspacing="0" border="0">
      <tbody>
    {% for key, url in allImages %}
      <tr>
       <td>{{ (key|e)+1 }}</td>
        <td>{{ url|e }}</td>
         </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
  <div class='info_options'>
<h3>Всего найдено {{allImages|length}} изображений</h3>
</div>
{{ form_start(form) }} 
{{ form_errors(form) }}
   {{ form_row(form.url) }} {{ form_row(form.script) }} 
    <input type = "submit" name = "submit" value = "Найти ссылки на изображения">   
{{ form_end(form) }}
</section>
</body>
</html>